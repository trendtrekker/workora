# Workora staging and merchant authentication — September 22, 2026

## External setup

- Shopify app `Workora` created with owner approval in Afro logics Ltd.
- App ID: `426370138113`; client ID: `f3238ae1bb1b19741d1e7d272d1fc052`.
- Settings: https://dev.shopify.com/dashboard/235134926/apps/426370138113/settings
- The automatically created app version still uses Shopify's placeholder URL. No store installation or app-data scopes were configured in this change.
- Neon project `proud-violet-01051839` matches Workora's configured production endpoint (`ep-calm-dawn-aep4ewbk`).
- Old staging branch was absent. Created replacement `workora-staging`, ID `br-sparkling-bread-aegamifv`, schema-only, with no auto-delete.
- Staging endpoint: `ep-sparkling-voice-aeucpah2-pooler.c-2.us-east-2.aws.neon.tech`.
- Applied migration 0004 only on the new staging branch. Production database was not modified.

## Verified

- `node scripts/verify-shopify-identity.mjs`: synthetic valid token accepted; forged signatures, wrong algorithms/audiences, expired/future timestamps, missing claims, wrong issuer/destination, and missing configuration rejected.
- `npx tsc --noEmit`: passed.
- `npm run build`: passed; existing CSS flex alignment warning remains.
- `node --env-file=.env.staging.local scripts/verify-staging-isolation.mjs`: passed tenant A/B isolation, empty-context denial, restricted-table denial and write denial.
- `node --env-file=.env.staging.local scripts/verify-merchant-access.mjs`: passed HTTP tests against the built Next.js server with real staging RLS. Missing/invalid tokens and cookies denied; caller tenant header ignored; A/B returned only their own merchant; unmapped and revoked installations denied. Temporary HTTP-test fixtures were removed.

The HTTP test uses synthetic Shopify credentials and signed tokens. It is not evidence of a real Shopify installation or App Bridge session.

## Remaining work

1. Configure a development app URL and the existing Shopify secret in ignored local/server configuration.
2. Implement App Bridge entry, token exchange and verified installation provisioning. Do not create installation mappings from unsigned shop parameters or accept tokens as proof of a completed install lifecycle.
3. Add verified uninstall handling and per-user authorization before exposing operational data/actions beyond the basic merchant overview.
4. Install into a development store and verify the live Shopify-to-Workora workflow, including uninstall/reinstall.

Local staging owner credential remains in `.env.staging.local`; the newly scoped merchant login is in `.env.merchant-staging-20260922.local`. Neither is tracked. `.env.local` still targets the existing founder environment; do not run tests against it.
